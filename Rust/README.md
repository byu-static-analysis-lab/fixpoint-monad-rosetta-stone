# Rust

Here is an example of how to implement the fixpoint monad in Rust.
There is also an 0-CFA Abstract Definitional Interpreter that uses the monad to serve as an example.
The ADI uses a lambda calculus like language and has a few tests to show that it works.